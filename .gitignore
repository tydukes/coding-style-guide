# macOS metadata and resource forks
# These files are created when storing on non-HFS+ volumes (external drives, network shares)
._*
.DS_Store
.AppleDouble
.LSOverride

# macOS Finder attributes
.Spotlight-V100
.Trashes

# macOS thumbnails
Thumbs.db
